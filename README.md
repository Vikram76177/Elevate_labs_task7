# Elevate_labs_task7
# Task 7 — Basic Sales Summary using SQLite & Python

This project connects Python to a SQLite database, runs basic SQL queries, 
and visualizes the results using a bar chart.

## Steps Covered
1. Created a SQLite database `sales_data.db`
2. Inserted sample sales data into a `sales` table
3. Wrote SQL query to calculate:
   - Total quantity sold per product
   - Total revenue per product
4. Loaded query result into pandas
5. Displayed summary using print()
6. Plotted a revenue bar chart using matplotlib

## Tools Used
- Python
- SQLite (sqlite3)
- Pandas
- Matplotlib

## Files in Repository
- `task7_code.ipynb`
- `sales_data.db`
- `sales_chart.png`
